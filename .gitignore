.aperture/
/bin/
coverage/
dist/
*.out
.atlas
.claude
# Bench fixtures are generator-materialized (see `make bench-prepare`);
# checking them in would bloat the repo without adding information.
/testdata/bench/small/
/testdata/bench/medium/
# Verifier artifacts (report.json, etc.) are produced by `verifier
# analyze` on demand; they're CI-only outputs, not source.
/.verifier/
